<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Book Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
    <!-- Update Book Container -->
    <div class="max-w-lg w-full bg-gray-800 rounded-lg shadow-lg p-6">
        <h1 class="text-3xl font-bold text-center mb-6">Update Book Details</h1>
        <form action="/updateBook" method="post" class="space-y-4" enctype="multipart/form-data">
            <!-- Hidden ID Field -->
            <input type="hidden" name="id" value="<%=data.id%>">
            
            <!-- Image URL -->
            <div>
                <label for="image" class="block mb-2 text-sm font-medium">Book Image URL</label>
                <input type="file" id="image" name="image" 
                       class="w-full p-2 rounded bg-gray-700 text-white border border-gray-600">
                <img src="/<%=data.image%>" class="w-[250px] h-[250px]" alt="">
            </div>
            
            <!-- Book Title -->
            <div>
                <label for="title" class="block mb-2 text-sm font-medium">Book Title</label>
                <input type="text" id="title" name="title" value="<%=data.title%>" 
                       placeholder="Enter Book Title" 
                       class="w-full p-2 rounded bg-gray-700 text-white border border-gray-600">
            </div>
            
            <!-- Author Name -->
            <div>
                <label for="author" class="block mb-2 text-sm font-medium">Author Name</label>
                <input type="text" id="author" name="author" value="<%=data.author%>" 
                       placeholder="Enter Author Name" 
                       class="w-full p-2 rounded bg-gray-700 text-white border border-gray-600">
            </div>
            
            <!-- Publish Date -->
            <div>
                <label for="publish" class="block mb-2 text-sm font-medium">Publish Date</label>
                <input type="date" id="publish" name="publish" value="<%=data.publish%>" 
                       placeholder="Enter Publish Date" 
                       class="w-full p-2 rounded bg-gray-700 text-white border border-gray-600">
            </div>
            
            <!-- Price -->
            <div>
                <label for="price" class="block mb-2 text-sm font-medium">Price</label>
                <input type="number" id="price" name="price" value="<%=data.price%>" 
                       placeholder="Enter Book Price" 
                       class="w-full p-2 rounded bg-gray-700 text-white border border-gray-600">
            </div>
            
            <!-- Copies Sold -->
            <div>
                <label for="copies" class="block mb-2 text-sm font-medium">Copies Sold</label>
                <input type="number" id="copies" name="copies" value="<%=data.copies%>" 
                       placeholder="Enter Copies Sold" 
                       class="w-full p-2 rounded bg-gray-700 text-white border border-gray-600">
            </div>
            
            <!-- Submit Button -->
            <button type="submit" 
                    class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-lg font-semibold">
                Update Book
            </button>
        </form>
    </div>
</body>
</html>
